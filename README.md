# FFmpeg Merge API Service

🎬 合併圖片和音樂成影片的 API 服務

## 功能

- 將靜態圖片和音樂合併成 MP4 影片
- 支援自訂解析度
- Base64 輸入/輸出
- RESTful API
- 健康檢查端點

## API 端點

### GET /health
健康檢查

### POST /api/merge
合併圖片和音樂

**請求範例:**
```json
{
  "audio_url": "https://example.com/audio.mp3",
  "image_data": "base64_encoded_image_string",
  "duration": "auto",
  "resolution": "1920x1080"
}
```

**回應範例:**
```json
{
  "success": true,
  "video_data": "base64_encoded_video",
  "size": 12345678,
  "size_mb": 11.77,
  "processing_time_ms": 15234,
  "processing_time_sec": 15.23
}
```

## 部署

### Zeabur
1. Fork 此 repository
2. 在 Zeabur 中新增服務
3. 選擇 Git 並連接此 repository
4. 自動檢測並構建

### Docker
```bash
docker build -t ffmpeg-api .
docker run -p 3000:3000 ffmpeg-api
```

## 授權

MIT License
```

點擊 `Commit changes`

---

## <a name="step2"></a>🚀 步驟 2：部署到 Zeabur

### **2.1 準備 Zeabur**

1. 登入 Zeabur：https://zeabur.com
2. 進入你的專案（與 n8n 相同的專案）

---

### **2.2 新增服務**

1. 點擊 **「新增服務」（Add Service）**
2. 選擇 **「Git」**
3. 點擊 **「Configure GitHub」** 授權 Zeabur 訪問你的 GitHub
4. 選擇你剛創建的 `ffmpeg-api-service` repository
5. 點擊 **「Import」**

---

### **2.3 等待構建**

Zeabur 會自動：
1. 檢測到 `Dockerfile`
2. 開始構建 Docker image
3. 部署服務

**構建時間**：約 5-10 分鐘（首次構建）

你可以點擊服務查看「**日誌**」標籤，看到構建過程。

---

### **2.4 確認部署成功**

當部署完成，日誌中會顯示：
```
═══════════════════════════════════════════════════════════
🎬 FFmpeg 合併服務已啟動
═══════════════════════════════════════════════════════════
📍 端口: 3000
🏥 健康檢查: http://localhost:3000/health
🔧 API 端點: http://localhost:3000/api/merge
📚 說明文檔: http://localhost:3000/
═══════════════════════════════════════════════════════════
```

---

### **2.5 查看服務名稱**

1. 在 Zeabur 中點擊你的 FFmpeg 服務
2. 查看「**網路**」標籤
3. 記下**內部域名**，通常是：
```
   ffmpeg-api-service.zeabur.internal
```
   或
```
   ffmpeg-merge-service.zeabur.internal
```

---

## <a name="step3"></a>🔧 步驟 3：在 n8n 中使用

### **3.1 測試 FFmpeg 服務**

在使用前，先測試服務是否正常。

在 n8n 中新增 **HTTP Request** 節點：

#### **節點名稱**：`測試 FFmpeg 健康檢查`
```
Method: GET
URL: http://ffmpeg-api-service.zeabur.internal:3000/health
